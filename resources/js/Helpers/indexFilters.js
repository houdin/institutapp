import filters from "./vueFilters";

const { Auth, Session, Image, Utility, Text, asset, config, asyncr } = filters;

export default filters;
export { Auth, Session, Image, Utility, Text, asset, config, asyncr };
